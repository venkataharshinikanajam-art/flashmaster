{
  "name": "flashmaster",
  "version": "0.1.0",
  "private": true,
  "description": "FLASHMASTER — Exam Helper App (MERN). Run both client and server with one command.",
  "scripts": {
    "dev": "concurrently -n \"server,client\" -c \"blue,magenta\" \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "start:server": "npm --prefix server run dev",
    "start:client": "npm --prefix client run dev",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
